// Re-export database types once generated
// export type { Database } from './database.types'

// Placeholder — database.types.ts will be generated after migrations
export type Database = Record<string, unknown>
